/** Simulation tick interval in milliseconds */
export const SIMULATION_TICK_MS = 2000;

/** Fuel % drained per tick = speed (km/h) × FUEL_DRAIN_FACTOR */
export const FUEL_DRAIN_FACTOR = 0.002;

/** Approximate degrees per km (1° latitude ≈ 111 km) */
export const EARTH_DEG_PER_KM = 1 / 111;
